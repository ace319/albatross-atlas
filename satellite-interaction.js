(()=>{const wrap=document.querySelector('.earth-wrap');const canvas=document.querySelector('#bird-earth');if(!wrap||!canvas)return;let dragging=false,lastX=0,position=50,zoom=100;function paint(){wrap.style.setProperty('--satellite-x',`${position}%`);wrap.style.setProperty('--satellite-zoom',`${zoom}%`)}canvas.addEventListener('pointerdown',event=>{dragging=true;lastX=event.clientX;wrap.classList.add('is-dragging');canvas.setPointerCapture(event.pointerId)});canvas.addEventListener('pointermove',event=>{if(!dragging)return;position=(position-(event.clientX-lastX)*.12+100)%100;lastX=event.clientX;paint()});canvas.addEventListener('pointerup',()=>{dragging=false;wrap.classList.remove('is-dragging')});canvas.addEventListener('pointercancel',()=>{dragging=false;wrap.classList.remove('is-dragging')});canvas.addEventListener('wheel',event=>{event.preventDefault();zoom=Math.max(100,Math.min(175,zoom+(event.deltaY>0?-8:8)));paint()},{passive:false});document.querySelectorAll('[data-earth-region]').forEach(button=>button.addEventListener('click',()=>{wrap.classList.remove('region-south','region-north','region-equator');wrap.classList.add(`region-${button.dataset.earthRegion}`);if(button.dataset.earthRegion==='south')position=50;if(button.dataset.earthRegion==='north')position=18;if(button.dataset.earthRegion==='equator')position=72;paint()}));paint()})();
